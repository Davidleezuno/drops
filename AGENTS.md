1. this is a hackathon - principle is to "focus on core feaetures and get those things very right, and then forget about the rest.
2. no need to write tests unless its necessary, and use hitpay sandbox
3. use supabase via secret key in .env.local, run migrations using pnpm db:generate or pnpm db:migrate
4. use pnpm